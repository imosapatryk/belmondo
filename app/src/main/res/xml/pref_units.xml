<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:key="units_speed"
        android:title="@string/pref_units_speed"
        android:entries="@array/speedUnits"
        android:entryValues="@array/speedUnits"
        android:defaultValue="-1"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        />
    <ListPreference
        android:key="units_distance"
        android:title="@string/pref_units_distance"
        android:entries="@array/distanceUnit"
        android:entryValues="@array/distanceUnit"
        android:defaultValue="-1"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        />
</PreferenceScreen>